<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bobo.mapper.RecordMapper">

    <select id="getRecordList" resultType="com.bobo.vo.RecordListVo">
        select record.id,record.recorder,record.date,record.time,user.user_avatar
        ,record.content,record.read_num
        from record, user
        where record.recorder= user.user_name
    </select>
</mapper>
